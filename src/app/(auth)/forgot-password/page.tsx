import { ForgotPasswordForm } from '@/components/auth/forgot-password-form';

export const metadata = {
  title: 'Reset Password — Proveground',
};

export default function ForgotPasswordPage() {
  return <ForgotPasswordForm />;
}
